/*global ARS, Promise */

var ARS;
ARS = ARS || {};
ARS.Filters = ARS.Filters || {};
ARS.Filters.FilterBase = (function () {
    "use strict";

    function FilterBase() {
        return undefined;
    }

    return FilterBase;
}());
